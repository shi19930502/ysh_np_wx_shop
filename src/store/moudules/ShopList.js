// initial state
const state = {
  added: [1],
  shopCarList: []
}

// getters
const getters = {
  getShopCarList: state => state.shopCarList
}

// actions
const actions = {
//   saveShopCarList({
//     commit
//   }) {
//     commit('saveShopCarList')
//   }
}

// mutations
const mutations = {
//   saveShopCarList(state) {
//     console.log(state.shopCarList)
//   }
}

export default {
  state,
  getters,
  actions,
  mutations
}
